import os
while True:
  userinput = input("Enter the application you want to open:")
    # os.system(userinput)
  if("run" in userinput) or ("google" in userinput) and ("chrome" in userinput):
    os.system("chrome")
  elif("run" in userinput)or("open" in userinput)or("execute" in userinput) and ("notepad" in userinput):
    os.system("notepad")
  elif("run" in userinput) or ("open" in userinput) or ("execute" in userinput) and ("paint" in userinput):
    os.system("mspaint")
  elif("run" in userinput) or ("open" in userinput) or ("execute" in userinput) and ("wordpad"in userinput):
    os.system("wordpad")
  elif("run" in userinput) or ("open" in userinput) or ("execute" in userinput) and ("media" in userinput) and ("player" in userinput):
    os.system("wmplayer")
  elif("run" in userinput) or ("open" in userinput) or ("execute" in userinput) and ("vlc" in userinput) and ("player" in userinput) and ("media" in userinput) and ("video" in userinput):
    os.system("vlc")
  elif("run" in userinput) or ("open" in userinput) or ("execute" in userinput) and ("calculator" in userinput) and ("calc" in userinput):
    os.system("start calculator")
  elif("run" in userinput) or ("open" in userinput) or ("execute" in userinput) and ("MSExcel" in userinput) and ("excel" in userinput):
    os.system("start excel")
  elif("run" in userinput) or ("open" in userinput) or ("execute" in userinput) and ("Edge" in userinput) and ("internet explorer" in userinput):
    os.system("msedge")
  elif("exit" in userinput) or ("quit" in userinput):
    break
  else:
    print("It won't work")
